version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: 'testProjectPwd'
    volumes:
      - 'C:/Users/aleksandra.lipskaya/Documents/test-project-postgres:/var/lib/postgresql/data'
